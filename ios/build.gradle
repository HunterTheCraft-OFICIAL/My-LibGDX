// ./ios/build.gradle [Gradle v8.11.1]-[Módulo iOS/RoboVM]

plugins {
    id "java"

    // 🔎 Suspeito: RoboVM (plugin para iOS, descontinuado)
    // Descomente para testar se ele força commons-compress:1.20
     id "org.robovm.gradle" version "2.3.12"
}

repositories {
    google()
    mavenCentral()
}

dependencies {
    // Nenhuma dependência LibGDX ainda — só o mínimo para testar o RoboVM
}